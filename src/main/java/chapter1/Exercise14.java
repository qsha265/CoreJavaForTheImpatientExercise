package chapter1;

import java.util.ArrayList;
import java.util.Scanner;

/**
 * chapter1.Exercise14
 *
 * @author Quanyu Sha
 * @date 05/03/2020
 */

public class Exercise14 {

    /* Question
    Write a program that reads a 2-dimensional array of integers
    and determines whether it is a magic square ( whether the sum of all rows, all columns, and the diagonals is the same).
    Accept lines of input that you break up into individual integers, and stop when the user enters a blank line.
    */

    public static void main(String[] args) {
        /* WIP
        int[][] twoDimensionalArray;
        ArrayList<String> inputsInString = new ArrayList<String>();
        Scanner input = new Scanner(System.in);
        System.out.println("Entry the integer array, entry a blank line to finish:");
        String line
        while(!input.nextLine().equals("")){
            String line = input.nextLine();
            inputsInString.add(line);
        }
        twoDimensionalArray = new int [inputsInString.size()] [];
        for (int i = 0; i < inputsInString.size(); i ++){
            String[] numStrings = inputsInString.get(i).split(" ");
            for(int j = 0; j < numStrings.length; j ++){
                twoDimensionalArray[i][j] = Integer.parseInt(numStrings[j]);
            }
        }  */
    }
}
